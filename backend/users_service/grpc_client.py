import grpc
# Placeholder client for calling hotel gRPC (if needed)
# Generated stubs would be required (compile proto files with protoc)
def check_room_availability_stub(hotel_id, room_type, nights):
    # This function is a placeholder to show where grpc client code would be.
    return True, "stubbed: available"
