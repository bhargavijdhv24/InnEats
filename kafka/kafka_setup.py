# Minimal helper: in local dev docker-compose this is not required.
# Use this file to programmatically create topics if using an admin client.
print('Kafka topics: booking_topic, order_topic (create via kafka manager or CLI if needed)')
